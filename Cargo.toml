[package]
name = "ahap_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
midly = { version = "0.5", default-features = false, features = ["std"] }
